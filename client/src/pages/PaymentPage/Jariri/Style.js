import { colors } from '../../../constants/colors'

export const JaririStyle = {
  color: {
    title: colors.mainColor,
    label: colors.darkColor,
  },
  button: {
    color: 'white',
    backgroundColor: colors.mainColor,
  },
}
